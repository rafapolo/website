# OS / editor
.DS_Store
*.log

# Tooling
CLAUDE.md
.claude/
tasks/
todo.md
node_modules/
.cache/
dist/
# CocoIndex Code (ccc)
/.cocoindex_code/

# Secrets
*.env
.env.*
*.pvt.txt
*.pub.txt
!0xCE8E94C8EDA640B6.pub.txt

# Private — job applications, CVs, cover letters, references. Keep out of the public repo.
job/
cvs/

# Local-only assets
img/

# projeto/ is local-only EXCEPT the three project folders the site actually links to.
# Everything else stays out — see todo.md.
projeto/*
!projeto/graficautopica.net/
!projeto/pensarpublico/
!projeto/subzku/

# ...and within those, keep the dead weight out:
projeto/pensarpublico/**/*.zip
projeto/subzku/subzku/
projeto/subzku/**/*.webm
projeto/subzku/**/*.ogg

# visitor access logs — contain IP addresses, must never be published
projeto/subzku/sub_access.txt
projeto/subzku/ips_count.txt
projeto/subzku/report.html
